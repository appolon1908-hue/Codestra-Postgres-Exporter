# Repository certification gate

This marker requests exact-head and synthetic merge-result validation for the dedicated Codestra PostgreSQL Exporter authority.

It does not create a PostgreSQL role, install credentials, deploy the exporter, publish port 9187, change firewalls, or activate a Prometheus target.
